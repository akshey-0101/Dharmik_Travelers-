import Link from 'next/link';import {site,waLink} from '@/lib/data';export default function Contact(){return <main><div className="container" style={{padding:'80px 0'}}><Link href="/">← Home</Link><div className="eyebrow" style={{marginTop:35}}>Contact</div><h1 style={{fontFamily:'Playfair Display',fontSize:56}}>हमसे बात करें</h1><p style={{fontSize:18,color:'var(--muted)',maxWidth:650,lineHeight:1.7}}>Upcoming yatra, seats, pickup points या package inclusions के बारे में पूछने के लिए WhatsApp सबसे तेज़ तरीका है.</p><div className="actions"><a className="btn primary" href={waLink('Namaste, mujhe upcoming Dharmik Yatra ki details chahiye.')}>WhatsApp</a><a className="btn secondary" href={`tel:${site.phone.replace(/[^0-9+]/g,'')}`}>Call {site.phone}</a></div></div></main>}
